<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Material.Icons.WinUI3.Demo.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Material.Icons.WinUI3.Demo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="using:System"
    xmlns:winUi3="using:Material.Icons.WinUI3"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance local:MainViewModel}">

  <Grid x:Name="Root">
    <Grid.Resources>
      <local:StringJoinConverter x:Key="StringJoinConverter" Separator="{x:Bind system:Environment.NewLine}" />
      <Style x:Key="SampleBorderStyle" TargetType="Border">
        <Setter Property="Margin" Value="10" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="CornerRadius" Value="5" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="Gray" />
      </Style>
      <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Margin" Value="0,0,0,5" />
      </Style>
    </Grid.Resources>

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <!-- Samples Section -->
    <Expander Grid.Row="0" Grid.ColumnSpan="2" Header="Icon Usage Examples" IsExpanded="True">
      <StackPanel Orientation="Vertical">
        <TextBlock Text="Different ways to use Material Icons in WinUI3" HorizontalAlignment="Center" 
                   Margin="0,5,0,10" FontSize="16" FontWeight="Bold"/>
        <ItemsRepeater HorizontalAlignment="Center">
          <ItemsRepeater.Layout>
            <StackLayout Orientation="Horizontal" Spacing="10" />
          </ItemsRepeater.Layout>
          <ItemsRepeater.Items>
            <!-- Sample 1: Basic MaterialIcon control -->
            <Border Style="{StaticResource SampleBorderStyle}">
              <StackPanel>
                <TextBlock Style="{StaticResource HeaderTextStyle}" Text="Basic MaterialIcon control"/>
                <winUi3:MaterialIcon Kind="Star" Width="32" Height="32"/>
                <TextBlock Text="&lt;materialIcons:MaterialIcon Kind=&quot;Star&quot; /&gt;" 
                           FontFamily="Consolas" Margin="0,5,0,0"/>
              </StackPanel>
            </Border>
            
            <!-- Sample 2: MaterialIcon with custom color -->
            <Border Style="{StaticResource SampleBorderStyle}">
              <StackPanel>
                <TextBlock Style="{StaticResource HeaderTextStyle}" Text="MaterialIcon with custom color"/>
                <winUi3:MaterialIcon Kind="Heart" Width="32" Height="32" Foreground="Red"/>
                <TextBlock Text="&lt;materialIcons:MaterialIcon Kind=&quot;Heart&quot; Foreground=&quot;Red&quot; /&gt;" 
                           FontFamily="Consolas" Margin="0,5,0,0"/>
              </StackPanel>
            </Border>
            
            <!-- Sample 3: MaterialIconExt markup extension -->
            <Border Style="{StaticResource SampleBorderStyle}">
              <StackPanel>
                <TextBlock Style="{StaticResource HeaderTextStyle}" Text="MaterialIconExt markup extension"/>
                <Button Content="{winUi3:MaterialIconExt Kind=Play}" Width="40" Height="40"/>
                <TextBlock Text="&lt;Button Content=&quot;{materialIcons:MaterialIconExt Kind=Play}&quot; /&gt;" 
                           FontFamily="Consolas" Margin="0,5,0,0"/>
              </StackPanel>
            </Border>
            
            <!-- Sample 4: MaterialIconTextExt markup extension -->
            <Border Style="{StaticResource SampleBorderStyle}">
              <StackPanel>
                <TextBlock Style="{StaticResource HeaderTextStyle}" Text="MaterialIconTextExt markup extension"/>
                <Button Content="{winUi3:MaterialIconTextExt Kind=Play, Text=Play}" Width="100" Height="40"/>
                <TextBlock Text="&lt;Button Content=&quot;{materialIcons:MaterialIconTextExt Kind=Play, Text=Play}&quot; /&gt;" 
                           FontFamily="Consolas" Margin="0,5,0,0" TextWrapping="Wrap"/>
              </StackPanel>
            </Border>
          </ItemsRepeater.Items>
        </ItemsRepeater>
      </StackPanel>
    </Expander>

    <!-- Original content -->
    <GridView
      Grid.Row="1"
      Grid.ColumnSpan="2"
      ItemsSource="{Binding Kinds.View}"
      SelectedItem="{Binding Group, Mode=TwoWay}">

      <GridView.ItemTemplate>
        <DataTemplate x:DataType="local:PackIconKindGroup">
          <Grid Width="64" Background="Transparent">
            <Grid.RowDefinitions>
              <RowDefinition Height="32" />
              <RowDefinition Height="32" />
            </Grid.RowDefinitions>
            <winUi3:MaterialIcon
              Grid.Row="0"
              Kind="{Binding Kind}"
              Width="32"
              Height="32" />
            <TextBlock
              Grid.Row="1"
              Text="{Binding DisplayName}"
              TextTrimming="CharacterEllipsis"
              HorizontalAlignment="Center" />
          </Grid>
        </DataTemplate>
      </GridView.ItemTemplate>
    </GridView>

    <TextBox Grid.Row="2" Grid.Column="0" Margin="8" Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    <TextBox Grid.Row="2" Grid.Column="1" Margin="8" IsReadOnly="True" Text="{Binding CopyText}" />
  </Grid>

</Window>
